import Chat from './container/Chat';
import Login from './container/Login';
import OpenChannels from './container/OpenChannels';

const routes = {
  login: Login,
  chat: Chat,
  openChannels: OpenChannels,
};

export default routes;
